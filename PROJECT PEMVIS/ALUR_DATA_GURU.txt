╔═══════════════════════════════════════════════════════════════════════════════╗
║                    ALUR DATA: FormDataGuru ↔ Database                          ║
╚═══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│  1. FORM LOAD - Data dari Database ke DataGridView                          │
└─────────────────────────────────────────────────────────────────────────────┘

   FormDataGuru.Load()
         │
         ├──> TampilData()
         │         │
         │         ├──> koneksi() → Open MySQL Connection
         │         │
         │         ├──> SELECT nip, nama_lengkap, mata_pelajaran, jenis_kelamin, alamat, no_telepon
         │         │    FROM tb_guru ORDER BY id_guru
         │         │
         │         └──> Loop dr.Read → DataGridView1.Rows.Add()
         │
         └──> DataGridView1 terisi data ✅


┌─────────────────────────────────────────────────────────────────────────────┐
│  2. BUTTON SIMPAN - Data dari TextBox ke Database                           │
└─────────────────────────────────────────────────────────────────────────────┘

   USER mengisi form:
         TextBox1 (NIP)           ──┐
         TextBox2 (Nama)          ──┤
         TextBox4 (Mapel)         ──┤
         RadioButton1/2 (JK)      ──┼──> Button1.Click()
         TextBox3 (Alamat)        ──┤            │
         TextBox5 (No Telp)       ──┘            │
                                                  ├──> koneksi()
                                                  │
                                                  ├──> INSERT INTO tb_guru
                                                  │    (nip, nama_lengkap, mata_pelajaran,
                                                  │     jenis_kelamin, alamat, no_telepon)
                                                  │    VALUES (@nip, @nama, @mapel, @jk, @alamat, @telp)
                                                  │
                                                  ├──> cmd.ExecuteNonQuery() → Simpan ke DB ✅
                                                  │
                                                  └──> TampilData() → Refresh DataGridView


┌─────────────────────────────────────────────────────────────────────────────┐
│  3. DATAGRIDVIEW CLICK - Data dari DataGridView ke TextBox                  │
└─────────────────────────────────────────────────────────────────────────────┘

   USER klik baris di DataGridView1
         │
         └──> DataGridView1.CellClick()
                    │
                    ├──> row.Cells(0) → TextBox1.Text (NIP)
                    ├──> row.Cells(1) → TextBox2.Text (Nama)
                    ├──> row.Cells(2) → TextBox4.Text (Mapel)
                    ├──> row.Cells(3) → RadioButton1/2 (JK)
                    ├──> row.Cells(4) → TextBox3.Text (Alamat)
                    └──> row.Cells(5) → TextBox5.Text (No Telp)
                    
                    Form terisi otomatis ✅


┌─────────────────────────────────────────────────────────────────────────────┐
│  4. BUTTON EDIT - Update data di Database                                   │
└─────────────────────────────────────────────────────────────────────────────┘

   USER klik baris → ubah TextBox → Button3.Click()
         │
         ├──> koneksi()
         │
         ├──> UPDATE tb_guru
         │    SET nama_lengkap=@nama, mata_pelajaran=@mapel,
         │        jenis_kelamin=@jk, alamat=@alamat, no_telepon=@telp
         │    WHERE nip=@nip
         │
         ├──> cmd.ExecuteNonQuery() → Update DB ✅
         │
         └──> TampilData() → Refresh DataGridView


┌─────────────────────────────────────────────────────────────────────────────┐
│  5. BUTTON HAPUS - Delete data dari Database                                │
└─────────────────────────────────────────────────────────────────────────────┘

   USER klik baris → Button4.Click()
         │
         ├──> MsgBox "Yakin hapus?"
         │         │
         │         └──> Yes
         │                │
         │                ├──> koneksi()
         │                │
         │                ├──> DELETE FROM tb_guru WHERE nip=@nip
         │                │
         │                ├──> cmd.ExecuteNonQuery() → Delete dari DB ✅
         │                │
         │                └──> TampilData() → Refresh DataGridView


┌─────────────────────────────────────────────────────────────────────────────┐
│  6. BUTTON CARI - Filter data dari Database                                 │
└─────────────────────────────────────────────────────────────────────────────┘

   USER ketik TextBox8 → Button6.Click()
         │
         ├──> koneksi()
         │
         ├──> SELECT nip, nama_lengkap, mata_pelajaran, jenis_kelamin, alamat, no_telepon
         │    FROM tb_guru
         │    WHERE nip LIKE '%keyword%' OR nama_lengkap LIKE '%keyword%'
         │
         └──> Loop dr.Read → DataGridView1 terfilter ✅


╔═══════════════════════════════════════════════════════════════════════════════╗
║                           DATABASE STRUCTURE                                   ║
╚═══════════════════════════════════════════════════════════════════════════════╝

Table: tb_guru
┌─────────────────┬──────────────────┬───────────────────────────────┐
│ Column          │ Type             │ Terhubung ke                  │
├─────────────────┼──────────────────┼───────────────────────────────┤
│ id_guru         │ INT (PK, AI)     │ Auto increment                │
│ nip             │ VARCHAR(50)      │ TextBox1 (Input/Output)       │
│ nama_lengkap    │ VARCHAR(100)     │ TextBox2 (Input/Output)       │
│ mata_pelajaran  │ VARCHAR(100)     │ TextBox4 (Input/Output)       │
│ jenis_kelamin   │ ENUM             │ RadioButton1/2 (Input/Output) │
│ alamat          │ TEXT             │ TextBox3 (Input/Output)       │
│ no_telepon      │ VARCHAR(20)      │ TextBox5 (Input/Output)       │
└─────────────────┴──────────────────┴───────────────────────────────┘


╔═══════════════════════════════════════════════════════════════════════════════╗
║                          KONEKSI DATABASE                                      ║
╚═══════════════════════════════════════════════════════════════════════════════╝

Connection String:
┌────────────────────────────────────────────────────────────┐
│  server=localhost;user=root;password=;database=db_sekolah  │
└────────────────────────────────────────────────────────────┘

Dimana:
  • server    : localhost (MySQL server lokal)
  • user      : root (default MySQL user)
  • password  : (kosong, no password)
  • database  : db_sekolah (nama database)


╔═══════════════════════════════════════════════════════════════════════════════╗
║                        ✅ STATUS: TERHUBUNG 100%                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝

✅ TextBox → Database: TERHUBUNG
✅ Database → DataGridView: TERHUBUNG
✅ DataGridView → TextBox: TERHUBUNG
✅ Button CRUD → Database: TERHUBUNG

FormDataGuru SIAP DIGUNAKAN! 🎉
